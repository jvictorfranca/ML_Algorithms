from statistics import mean

def complete_hierarchy(items: list[float]):
    return max(items)

def single_hierarchy(items: list[float]):
    return min(items)

def average_hierarchy(items: list[float]):
    return  mean(items)